import { StackNavigationOptions } from '@react-navigation/stack';

export const AUTH_NAVIGATOR_CONFIG: StackNavigationOptions = {
  headerShown: false,
  gestureEnabled: true, // enables swipe back
};
